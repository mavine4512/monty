#include "monty.h"
/**
 * f_stack - a program function that prints memory of stack
 * @head: top element of stack
 * @count: counts elements in a stack
 * Return: top element
 */
void f_stack(stack_t **head, unsigned int count)
{
	(void)head;
	(void)count;
	bus.lifi = 0;
}

